#include "main.h"

/**
 * print_rev - prints a string, in reverse
 * @s: the string
 * Return: None
 */

void print_rev(char *s)
{
	int i = 0;

	for (; s[i] != '\0'; i++)
	{
	}
	while (i >= 0)
	{
		_putchar(s[l]);
		i--;
	}
	_putchar('\n');
}
